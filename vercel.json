{
  "version": 2,
  "build": {
    "env": {
      "CI": "false"
    }
  },
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
